language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-sweetjs-support' ]; then cd .. && eval "mv $currentfolder generator-sweetjs-support" && cd generator-sweetjs-support; fi

